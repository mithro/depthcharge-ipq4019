# Build artifacts
/build/
*.o
*.a
*.elf
*.bin
*.payload
*.rom
*.map
*.tmp

# depthcharge is vendored as a submodule (see .gitmodules) — NOT ignored.
# Other upstream trees are fetched for building/reference (not vendored):
/u-boot/
/openwrt/
/coreboot/
/chromiumos/

# Editor / OS
*.swp
*~
.DS_Store

# Additional upstream trees
/vboot_reference/
/flashrom-cros/

# Build/clone logs in repo root
/tmp_coreboot_clone.log

# Runtime artifacts in tmp/
/tmp/tftproot/
/tmp/*.log
/tmp/*.leases
/tmp/*.pcap
/tmp/*.pid
# Regenerable build outputs (FIT images, compiled DTBs, raw blobs)
/tmp/*.itb
/tmp/*.dtb
/tmp/*.bin
